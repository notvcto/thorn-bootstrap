fn extract_var_name(line) {
  let parts = line.split("=")
  return parts[0].trim().replace("let ", "")
}

fn extract_var_value(line) {
  let parts = line.split("=")
  return parts[1].trim()
}

fn extract_print_arg(line) {
  return line.trim().remove_prefix("print(").remove_suffix(")")
}

# thorn.thorn

# Declare the environment (just symbolic for now)
let env = {}

fn eval(expr) {
    # This is handled by runner.py for now
}

fn exec(line) {
    # This is handled by runner.py for now
}

fn run_file(path) {
    let content = read(path)
    let lines = content.split("\n")
    for line in lines {
        # Placeholder for runner.py to execute lines
    }
}
